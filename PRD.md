# PRD：飞牛增强层 — 全自动入库系统

> 版本：v0.1 初稿 | 状态：需求确认中

---

## 一、项目概述

### 核心问题
现有入库流程断裂：TG 发链接后需要手动识别片名、手动改名、手动移动到飞牛影视能识别的目录结构。改名不规范导致飞牛刮削失败。

### 目标用户
自己（NAS 管理员）+ 家人（只想在飞牛影视里看到能看的片，不想折腾）

### 核心价值
**TG 发一个链接 → 全自动完成识别/转存/改名/入库 → 飞牛影视海报墙直接出现**

---

## 二、用户故事

### 主流程（必须）
```
作为 NAS 管理员
我希望在 TG 群里发一个夸克/百度/光鸭分享链接
系统自动完成：
  1. 识别链接类型（夸克/百度/光鸭）
  2. 从分享链接中提取文件列表
  3. 识别片名（文件名/目录名/AI推断）
  4. 转存到我的网盘
  5. 按规范格式改名：
     电影：片名 (年份)/片名 (年份).ext
     剧集：片名 (年份)/Season XX/片名 - SXXEXX.ext
  6. 放到飞牛影视能扫描的目录
  7. TG 回复确认消息（片名、路径、海报预览）
```

### 辅助功能（重要）
```
- /search 片名 → 用 PanSou 搜索资源，返回可选列表
- /追剧 片名 → 订阅剧集，有新集自动入库
- /status → 查看入库队列和最近入库记录
- /retry 任务ID → 重试失败的入库任务
```

### 锦上添花（可选）
- 字幕自动下载（从射手/OpenSubtitles）
- 入库后自动通知飞牛刷新媒体库
- 重复检测（已入库的片不再转存）
- 多链接批量处理

---

## 三、改名规范（核心）

### 电影
```
目标目录结构：
影视/
├── 电影/
│   ├── 肖申克的救赎 (1994)/
│   │   ├── 肖申克的救赎 (1994).mkv
│   │   └── 肖申克的救赎 (1994).srt        ← 字幕（如有）
│   ├── 盗梦空间 (2010)/
│   │   └── 盗梦空间 (2010).mp4
│   └── ...

文件名规则：中文片名 (年份).扩展名
目录名规则：中文片名 (年份)
```

### 电视剧
```
影视/
├── 电视剧/
│   ├── 狂飙 (2023)/
│   │   ├── Season 01/
│   │   │   ├── 狂飙 - S01E01.mkv
│   │   │   ├── 狂飙 - S01E02.mkv
│   │   │   └── ...
│   │   └── poster.jpg                      ← 海报（如有）
│   ├── 三体 (2023)/
│   │   ├── Season 01/
│   │   │   ├── 三体 - S01E01.mp4
│   │   │   └── ...
│   │   └── ...
│   └── ...

文件名规则：中文片名 - SXXEXX.ext
目录规则：中文片名 (年份)/Season XX/
```

### 综艺/纪录片
```
同电视剧结构，Season 按年份或季数分
```

### 识别逻辑优先级
1. 分享链接的目录名（最可靠）
2. 视频文件名（次可靠）
3. 以上都没有 → AI 推断（用 DeepSeek 从文件名猜片名）
4. 年份：TMDB 查中文片名匹配年份

---

## 四、技术方案

### 架构
```
TG Bot（接收链接）
    ↓
入库引擎（Go，核心逻辑）
    ├── 链接识别 → 判断网盘类型
    ├── 文件列表 → 获取分享中的文件
    ├── 片名识别 → 文件名解析 + TMDB 匹配 + AI 兜底
    ├── 转存执行 → CloudDrive2 API 或网盘 API
    ├── 改名引擎 → 按规范重命名 + 移动
    └── 通知模块 → TG 回复结果
    ↓
飞牛影视扫描目录（/vol2/1000/CloudDrive/影视/）
```

### 网盘对接
| 网盘 | 转存方式 | 改名方式 |
|------|---------|---------|
| 夸克 | 夸克 API 转存 | CloudDrive2 挂载后直接 mv |
| 百度 | 百度 API 转存 | CloudDrive2 挂载后直接 mv |
| 光鸭 | 光鸭 API 离线秒存 | CloudDrive2 挂载后直接 mv |

### 改名引擎
- **输入**：原始文件名（可能乱七八糟，如 `[www.xxx.com]吞噬星空.第81集.HD1080P.国语中字.mp4`）
- **处理**：
  1. 去除广告标记 `[xxx]`、`(xxx.com)` 等
  2. 提取片名和集数（正则 + 模式匹配）
  3. TMDB 查询确认中文名和年份
  4. 按规范格式化输出文件名
- **输出**：规范的文件名和目录路径

### TMDB 匹配
- 使用 TMDB API 查询片名
- 优先中文名匹配
- 支持别名匹配（如"吞噬星空" = "Swallowed Star"）
- 缓存已匹配的片名，减少 API 调用

### 技术栈
- **语言**：Go（你已有 Go 环境，panlink 也是 Go 写的）
- **框架**：Telegram Bot API（go-telegram）
- **存储**：SQLite（记录入库历史、任务队列、片名缓存）
- **部署**：Docker Compose（与现有容器统一管理）
- **配置**：YAML + 环境变量

---

## 五、飞牛影视对接

### 飞牛扫描目录
飞牛影视需要配置媒体库目录。入库系统将文件放到：
```
/vol2/1000/CloudDrive/影视/电影/   ← 飞牛扫描"电影"库
/vol2/1000/CloudDrive/影视/电视剧/  ← 飞牛扫描"电视剧"库
```

### 刷新机制
- 方案 A：飞牛自带定时扫描（最简单，延迟几分钟）
- 方案 B：入库完成后调用飞牛 API 刷新媒体库（需研究飞牛 API）
- 建议先用 A，后续加 B

---

## 六、验收标准

### P0（必须通过）
- [ ] TG 发一个夸克分享链接 → 自动转存、改名、入库 → 飞牛影视能看到
- [ ] TG 发一个百度分享链接 → 同上
- [ ] TG 发一个光鸭分享链接 → 同上
- [ ] 改名格式符合规范（电影/电视剧分开，含年份/季集）
- [ ] 文件名包含广告标记时能自动清除
- [ ] 入库后 TG 收到确认消息（片名、路径）
- [ ] 重复链接不重复入库

### P1（应该有）
- [ ] /search 搜索功能可用
- [ ] 失败任务有重试机制
- [ ] 入库历史可查询
- [ ] 批量链接处理

### P2（可以后做）
- [ ] 自动追剧订阅
- [ ] 字幕下载
- [ ] 飞牛 API 刷新

---

## 七、与现有系统的关系

| 现有组件 | 本项目如何处理 |
|---------|-------------|
| tg-media-bot | 复用 TG Bot 框架，重写入库逻辑 |
| PanSou | 作为 /search 的后端，不改动 |
| CloudDrive2 | 作为文件系统层，本项目直接操作挂载目录 |
| 飞牛影视 | 本项目只管"把文件放对位置"，刮削和播放由飞牛负责 |
| panlink | 参考其网盘 API 调用逻辑，但本项目专注入库改名 |

---

## 八、里程碑

| 阶段 | 内容 | 预计时间 |
|------|------|---------|
| M1 | PRD 确认 + 技术方案评审 | 今天 |
| M2 | 改名引擎（核心）：原始文件名 → 规范文件名 | 1-2 天 |
| M3 | 链接识别 + 网盘转存 | 2-3 天 |
| M4 | TG Bot 集成 + 端到端跑通 | 1-2 天 |
| M5 | 飞牛对接 + 入库验证 | 1 天 |
| M6 | 搜索/追剧/字幕等增强功能 | 后续迭代 |

---

## 九、开放问题

1. **飞牛影视的媒体库目录是 `/vol2/1000/CloudDrive/影视/` 吗？** 还是其他路径？
2. **tg-media-bot 现在的代码还在吗？** 还是已经弃用了？
3. **你希望 TG Bot 是独立容器还是集成到现有 media-center？**
4. **TMDB API Key 有了吗？**（tg-yimao-bot 里应该有）
5. **光鸭的 API 调用方式是怎样的？** 之前 panlink 里的逻辑能复用吗？
